#include "types.h"
#include "kernel.h"

extern PrintString();

void kernel() {
	
    init_syscalls();
	//interrupt(0x80,0,99,10,0);
	interrupt(0x80,0,99,10,0);
	while(1);
}


extern void syscall_handler(uint syscall_nb, uint arg1, uint arg2, uintarg3, arg4) 
{


switch (syscall_nb)
{

	case 0: {
		//pipo();
		print_char(99);
		//interrupt(0x10,65,0,10,0);
		break;
		}

	case 1: {
		//pipo();
		//interrupt(0x10,65,0,10,0);
		break;
		}


}

	
}


void print_char(char c){
	pipo();	
	//interrupt(0x10,65,0,10,0,0);
	// interrupt(0x80,0,65,10,0,0);
	
}
